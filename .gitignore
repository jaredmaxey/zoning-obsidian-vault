# Obsidian volatile state (machine-specific, changes constantly)
.obsidian/workspace.json
.obsidian/graph.json

# Source PDFs: 892MB total, some exceed GitHub's 100MB hard limit.
# The source layer is versioned via 40-data/source_registry.csv and the .url files.
Official Sources/**/*.pdf
